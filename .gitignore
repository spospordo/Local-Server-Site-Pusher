node_modules/
npm-debug.log*
.npm
.env
.DS_Store
*.log
coverage/
.nyc_output/
dist/
build/
tmp/

# Static files backup (created by Dockerfile, only needed in Docker image)
.static-files-backup/

# Runtime configuration files (keep directory structure)
config/config.json

# Finance module sensitive data (encrypted but should not be in repo)
config/.finance_key
config/.finance_data
config/.client_auth

# Magic Mirror sensitive data (encrypted but should not be in repo)
config/magicmirror-config.json.enc
config/.magicmirror-key

# Backup files generated by scripts
backups/

# Client uploaded files
uploads/cookies.jar

# Uploaded template files from testing (these are test artifacts)
uploads/espresso/templates/
config/.gitconfig
